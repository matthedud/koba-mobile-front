import React from "react"

import { DeleteOutlined } from '@ant-design/icons'

const deleteButton = <span><div 
    style={{cursor:'pointer'}}>
        <DeleteOutlined style={{color:'red', fontSize:'1.5rem'}}/>
</div></span>

export default deleteButton