import React from 'react'
import './ButtonGroupe.css'

const ButtonFoorterGroupe = props => {
  return (
    <div className='button-groupe button-footer'>{props.children}</div>
  )
}

export default ButtonFoorterGroupe